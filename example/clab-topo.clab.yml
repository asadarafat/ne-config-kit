---
name: ne-config-kit-example

# Minimal containerlab topology: one SR Linux device.
# Run the NCK container separately (see README).

topology:
  nodes:
    srl1:
      kind: nokia_srlinux
      image: ghcr.io/nokia/srlinux:latest

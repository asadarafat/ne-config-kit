---
# Example inventory for the containerlab topology in example/clab-topo.clab.yml
all:
  children:
    routers:
      children:
        nokia_srlinux:
          hosts:
            srl1:
              ansible_host: 172.20.20.5
              ansible_network_os: nokia.srlinux.srlinux
